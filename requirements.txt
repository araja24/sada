# Backend web framework (M3)
fastapi
uvicorn[standard]
python-multipart

# Audio analysis core (M1, M2)
librosa
numpy
scipy
fastdtw
pydub
imageio-ffmpeg  # bundles a static ffmpeg binary; lets hosts without system ffmpeg (Render native runtime) work with no Docker -- see analysis/audio_io.py
matplotlib
soundfile

# Quran Foundation API client + config (M1)
requests
python-dotenv

# Persistence (M3)
sqlalchemy

# Auth (M3) -- see docs/adr/0002-user-accounts.md
bcrypt
itsdangerous

# Dev / testing
pytest
httpx  # required by fastapi.testclient
